branches:
  only:
    - master

environment:
  matrix:
  - example: questia-eng
    name: win32
    platform: mingw

clone_folder: c:\build

before_build:
  - set Path=C:\mingw-w64\bin;%Path%
  - g++ --version
  - mingw32-make --version

build_script:
  - cd c:\build
  - ps: mingw32-make test